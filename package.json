{
  "name": "hexcrawl",
  "version": "1.0.0",
  "description": "HexCrawl Map Tracker - collaborative TTRPG hex map",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
